<UserControl
    x:Class="SihyuPOSPayroll.Views.Admin.Users.AddEditUser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignWidth="400" d:DesignHeight="400"
    Background="#AA000000">

    <Border
        Background="#090B17"
        BorderBrush="#072219"
        BorderThickness="1"
        CornerRadius="12"
        Width="420"
        Padding="28"
        VerticalAlignment="Center"
        HorizontalAlignment="Center">

        <StackPanel>
            <!-- Title -->
            <TextBlock x:Name="TitleText"
                       Text="Add / Edit User"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#477C60"
                       Margin="0,0,0,20"
                       TextAlignment="Center" />

            <!-- Email -->
            <TextBlock Text="Email:" FontWeight="SemiBold" Foreground="#9CA3AF"/>
            <TextBox x:Name="EmailTextBox" Margin="0,4,0,14" />

            <!-- Password -->
            <TextBlock Text="Password:" FontWeight="SemiBold" Foreground="#9CA3AF"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,4,0,14" />

            <!-- Role -->
            <TextBlock Text="Role:" FontWeight="SemiBold" Foreground="#9CA3AF"/>
            <ComboBox x:Name="RoleComboBox" Margin="0,4,0,14" ItemsSource="{Binding Roles}" />

            <!-- Linked Employee -->
            <TextBlock Text="Linked Employee (optional):" FontWeight="SemiBold" Foreground="#9CA3AF"/>
            <ComboBox x:Name="EmployeeComboBox"
                      Margin="0,4,0,20"
                      DisplayMemberPath="FullName"
                      SelectedValuePath="Id"
                      ItemsSource="{Binding Employees}" />

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel"
                        Margin="0,0,10,0"
                        Style="{StaticResource SecondaryButton}"
                        Click="Cancel_Click" />
                <Button x:Name="SaveButton"
                        Content="Save"
                        Style="{StaticResource PrimaryButton}"
                        Click="Save_Click" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
