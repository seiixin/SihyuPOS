<UserControl
    x:Class="SihyuPOSPayroll.Views.Admin.Menu.MenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:HillsCafeManagement.ViewModels"
    xmlns:models="clr-namespace:HillsCafeManagement.Models"
    mc:Ignorable="d">
    
    <UserControl.DataContext>
        <viewmodels:MenuViewModel />
    </UserControl.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Text="Menu Management" FontSize="24" FontWeight="Bold" Margin="20" HorizontalAlignment="Center"/>
        
        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Form Panel -->
                <Border Grid.Column="0" BorderBrush="LightGray" BorderThickness="1" Padding="20" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="Menu Item Details" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                        
                        <TextBlock Text="Name:" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NewItemName, UpdateSourceTrigger=PropertyChanged}" 
                                 Margin="0,0,0,10" Padding="5"/>
                        
                        <TextBlock Text="Category:" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NewItemCategory, UpdateSourceTrigger=PropertyChanged}" 
                                 Margin="0,0,0,10" Padding="5"/>
                        
                        <TextBlock Text="Price:" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NewItemPrice, UpdateSourceTrigger=PropertyChanged}" 
                                 Margin="0,0,0,10" Padding="5"/>
                        
                        <TextBlock Text="Description:" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NewItemDescription, UpdateSourceTrigger=PropertyChanged}" 
                                 Margin="0,0,0,15" Padding="5" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Content="Add Item" Command="{Binding AddCommand}" 
                                    Margin="0,0,10,0" Padding="15,5" MinWidth="80"/>
                            <Button Content="Update Item" Command="{Binding UpdateCommand}" 
                                    Margin="0,0,10,0" Padding="15,5" MinWidth="80"/>
                            <Button Content="Clear" Command="{Binding ClearFormCommand}" 
                                    Padding="15,5" MinWidth="80"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <!-- Items List Panel -->
                <Border Grid.Column="1" BorderBrush="LightGray" BorderThickness="1" Padding="20" Margin="10,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Menu Items" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                        
                        <DataGrid ItemsSource="{Binding MenuItems}" 
                                  SelectedItem="{Binding SelectedMenuItem}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  SelectionMode="Single"
                                  GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column"
                                  AlternatingRowBackground="LightGray">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="80" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200" IsReadOnly="True"/>
                                <DataGridTemplateColumn Header="Actions" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Delete" 
                                                    Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                    CommandParameter="{Binding}"
                                                    Background="Red" 
                                                    Foreground="White" 
                                                    Padding="5,2" 
                                                    Margin="2"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>