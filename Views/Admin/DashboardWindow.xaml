<Window x:Class="SihyuPOSPayroll.Views.Admin.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Dashboard - Sihyu POS & Payroll"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        ResizeMode="CanResize"
        Background="{StaticResource BackgroundDarkBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Background="{StaticResource SurfaceDarkBrush}"
                BorderBrush="{StaticResource SurfaceLighterBrush}"
                BorderThickness="0,0,1,0">
            <StackPanel Margin="20">
                <!-- Logo/Branding -->
                <TextBlock Text="Sihyu POS & Payroll"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{StaticResource AccentEmeraldBrush}"
                           Margin="0,10,0,30"/>

                <!-- User Profile -->
                <Border Background="{StaticResource BackgroundDarkBrush}"
                        CornerRadius="8"
                        Padding="16"
                        Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Ellipse Width="48" Height="48" Fill="{StaticResource SurfaceLighterBrush}" Grid.Column="0"/>
                        <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="ADMIN" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14"/>
                            <TextBlock Text="Admin User" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Navigation List -->
                <ListBox Background="Transparent"
                         BorderThickness="0"
                         Padding="0">
                    <ListBoxItem Content="Dashboard"/>
                    <ListBoxItem Content="Users"/>
                    <ListBoxItem Content="Employees"/>
                    <ListBoxItem Content="Payroll"/>
                    <ListBoxItem Content="Payslip Requests"/>
                    <ListBoxItem Content="Attendance"/>
                    <ListBoxItem Content="Menu"/>
                    <ListBoxItem Content="Inventory"/>
                    <ListBoxItem Content="Orders"/>
                    <ListBoxItem Content="Receipts"/>
                    <ListBoxItem Content="Tables"/>
                    <ListBoxItem Content="Sales &amp; Reports"/>
                    <ListBoxItem Content="Logout"/>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Border Grid.Column="1" Background="{StaticResource BackgroundDarkBrush}" Padding="24">
            <TextBlock Text="Select a page from the sidebar"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="18"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
