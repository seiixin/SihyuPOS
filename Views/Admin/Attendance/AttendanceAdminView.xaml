<UserControl x:Class="SihyuPOSPayroll.Views.Admin.Attendance.AttendanceAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:SihyuPOSPayroll.Helpers"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:SihyuPOSPayroll.Models"
             mc:Ignorable="d"
             Background="#000000"
             d:DesignHeight="600"
             d:DesignWidth="900">

    <UserControl.Resources>
        <helpers:EmptyStringToVisibilityConverter x:Key="EmptyStringToVisibilityConverter" />

        <x:Array x:Key="EditableStatuses" Type="{x:Type models:LeaveStatus}">
            <models:LeaveStatus>Pending</models:LeaveStatus>
            <models:LeaveStatus>Approved</models:LeaveStatus>
            <models:LeaveStatus>Rejected</models:LeaveStatus>
        </x:Array>

        <Style x:Key="CellCombo" TargetType="ComboBox">
            <Setter Property="IsEditable" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>
        <Style x:Key="CellComboEditing" TargetType="ComboBox" BasedOn="{StaticResource CellCombo}">
            <Setter Property="SelectedItem"
                    Value="{Binding Status, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </Style>

        <!-- ── Styled TabItem so both tabs look dark/green ── -->
        <Style x:Key="DarkTabItem" TargetType="TabItem">
            <Setter Property="Foreground"       Value="#9CA3AF"/>
            <Setter Property="Background"       Value="Transparent"/>
            <Setter Property="BorderThickness"  Value="0"/>
            <Setter Property="Padding"          Value="18,10"/>
            <Setter Property="FontSize"         Value="14"/>
            <Setter Property="FontFamily"       Value="Segoe UI"/>
            <Setter Property="Cursor"           Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="0,0,0,2"
                                Padding="{TemplateBinding Padding}">
                            <TextBlock x:Name="TabText"
                                       Text="{TemplateBinding Header}"
                                       Foreground="{TemplateBinding Foreground}"
                                       FontSize="{TemplateBinding FontSize}"
                                       FontFamily="{TemplateBinding FontFamily}"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="#128854"/>
                                <Setter TargetName="TabText"   Property="Foreground"  Value="#F3F4F6"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabText" Property="Foreground" Value="#F3F4F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── Kill the inner DatePickerTextBox rectangle ── -->
        <Style x:Key="CleanDatePickerTextBox" TargetType="DatePickerTextBox">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="10,0"/>
            <Setter Property="Foreground"      Value="#F3F4F6"/>
            <Setter Property="CaretBrush"      Value="#128854"/>
            <Setter Property="SelectionBrush"  Value="#128854"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePickerTextBox">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                            <!-- Watermark -->
                            <ContentControl x:Name="PART_Watermark"
                                            Content="{TemplateBinding Tag}"
                                            Foreground="#6B7280"
                                            Focusable="False"
                                            IsHitTestVisible="False"
                                            Margin="{TemplateBinding Padding}"
                                            VerticalAlignment="Center"
                                            Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="PART_Watermark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="PART_Watermark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── Styled DatePicker (dark bg, green focus, no inner rectangle) ── -->
        <Style x:Key="DarkDatePicker" TargetType="DatePicker">
            <Setter Property="Background"      Value="#090B17"/>
            <Setter Property="Foreground"      Value="#F3F4F6"/>
            <Setter Property="BorderBrush"     Value="#1A1D2E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="0"/>
            <Setter Property="Height"          Value="36"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="32"/>
                                </Grid.ColumnDefinitions>

                                <!-- Inner text input — styled to have NO border/rectangle -->
                                <DatePickerTextBox x:Name="PART_TextBox"
                                                   Grid.Column="0"
                                                   Style="{StaticResource CleanDatePickerTextBox}"
                                                   Tag="Select a date"
                                                   Foreground="{TemplateBinding Foreground}"/>

                                <!-- Calendar icon -->
                                <Button x:Name="PART_Button"
                                        Grid.Column="1"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Focusable="False"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center">
                                    <TextBlock Text="&#xE787;"
                                               FontFamily="Segoe MDL2 Assets"
                                               FontSize="13"
                                               Foreground="#477C60"/>
                                </Button>

                                <!-- Calendar popup -->
                                <Popup x:Name="PART_Popup"
                                       Grid.Column="0"
                                       AllowsTransparency="True"
                                       Placement="Bottom"
                                       PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                       StaysOpen="False"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush"     Value="#128854"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── Styled TextBox for filter inputs ── -->
        <Style x:Key="FilterTextBox" TargetType="TextBox">
            <Setter Property="Background"       Value="#090B17"/>
            <Setter Property="Foreground"       Value="#F3F4F6"/>
            <Setter Property="BorderBrush"      Value="#1A1D2E"/>
            <Setter Property="BorderThickness"  Value="1"/>
            <Setter Property="Padding"          Value="10,0"/>
            <Setter Property="Height"           Value="36"/>
            <Setter Property="FontSize"         Value="13"/>
            <Setter Property="FontFamily"       Value="Segoe UI"/>
            <Setter Property="CaretBrush"       Value="#128854"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Center"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush"     Value="#128854"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Margin="28,24,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ── Page Header ── -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border BorderBrush="#072219" BorderThickness="0,0,0,1" Margin="0,0,0,20" Padding="0,0,0,14">
                <TextBlock Text="Attendance &amp; Leave Admin"
                           FontSize="26" FontWeight="Bold" Foreground="#477C60"
                           VerticalAlignment="Center"/>
            </Border>
            <Button Grid.Column="1"
                    Content="Work Schedules"
                    Style="{StaticResource PrimaryButton}"
                    Margin="0,0,0,20"
                    Click="OpenWorkSchedules_Click"
                    VerticalAlignment="Bottom"/>
        </Grid>

        <!-- ── Tab control ── -->
        <TabControl Grid.Row="1"
                    Background="#000000"
                    BorderBrush="#072219"
                    BorderThickness="0,1,0,0"
                    Padding="0,8,0,0">

            <!-- ══ ATTENDANCE TAB ══ -->
            <TabItem Header="Attendance" Style="{StaticResource DarkTabItem}">
                <Grid Background="Transparent" Margin="0,14,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Filter bar -->
                    <Grid Grid.Row="0" Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                            <TextBlock Text="Date:"
                                       Foreground="#9CA3AF"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>
                            <DatePicker SelectedDate="{Binding FilterDate, Mode=TwoWay}"
                                        Width="160"
                                        Style="{StaticResource DarkDatePicker}"
                                        Margin="0,0,20,0"/>

                            <TextBlock Text="Employee ID:"
                                       Foreground="#9CA3AF"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>
                            <TextBox Text="{Binding FilterEmployeeId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Width="130"
                                     Style="{StaticResource FilterTextBox}"
                                     Margin="0,0,0,0"/>
                        </StackPanel>

                        <Button Grid.Column="1"
                                Content="Filter"
                                Command="{Binding FilterCommand}"
                                Style="{StaticResource PrimaryButton}"/>
                    </Grid>

                    <!-- Attendance grid -->
                    <Border Grid.Row="1" Background="#090B17" CornerRadius="10" Padding="0">
                        <DataGrid ItemsSource="{Binding Attendances}"
                                  SelectedItem="{Binding SelectedAttendance, Mode=TwoWay}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="False"
                                  BorderThickness="0"
                                  Background="#090B17"
                                  GridLinesVisibility="None"
                                  RowHeight="48"
                                  ColumnHeaderHeight="44">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID"          Binding="{Binding Id}"                                              IsReadOnly="True" Width="*"/>
                                <DataGridTextColumn Header="Employee ID" Binding="{Binding EmployeeId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"    Width="*"/>
                                <DataGridTextColumn Header="Date"        Binding="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}}"                              Width="*"/>
                                <DataGridTextColumn Header="Time In"     Binding="{Binding TimeIn, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"         Width="*"/>
                                <DataGridTextColumn Header="Time Out"    Binding="{Binding TimeOut, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"        Width="*"/>
                                <DataGridTextColumn Header="Status"      Binding="{Binding Status, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"         Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <!-- CRUD buttons -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                        <Button Content="Add"    Command="{Binding AddCommand}"    Style="{StaticResource PrimaryButton}"   Margin="0,0,8,0"/>
                        <Button Content="Update" Command="{Binding UpdateCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>
                        <Button Content="Delete" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButton}"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- ══ LEAVE REQUESTS TAB ══ -->
            <TabItem Header="Leave Requests" Style="{StaticResource DarkTabItem}">
                <Grid Background="Transparent" Margin="0,14,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Filter bar -->
                    <Grid Grid.Row="0" Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                            <TextBlock Text="Employee ID:"
                                       Foreground="#9CA3AF"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>
                            <TextBox Text="{Binding LeaveFilterEmployeeId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Width="120"
                                     Style="{StaticResource FilterTextBox}"
                                     Margin="0,0,20,0"/>

                            <TextBlock Text="From:"
                                       Foreground="#9CA3AF"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>
                            <DatePicker SelectedDate="{Binding LeaveFrom, Mode=TwoWay}"
                                        Width="160"
                                        Style="{StaticResource DarkDatePicker}"
                                        Margin="0,0,20,0"/>

                            <TextBlock Text="To:"
                                       Foreground="#9CA3AF"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>
                            <DatePicker SelectedDate="{Binding LeaveTo, Mode=TwoWay}"
                                        Width="160"
                                        Style="{StaticResource DarkDatePicker}"
                                        Margin="0,0,20,0"/>

                            <TextBlock Text="Status:"
                                       Foreground="#9CA3AF"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>
                            <ComboBox SelectedValue="{Binding LeaveFilterStatus, Mode=TwoWay}"
                                      SelectedValuePath="Tag"
                                      Width="140"
                                      Height="36"
                                      Margin="0,0,0,0">
                                <ComboBoxItem Content="Pending"  Tag="{x:Static models:LeaveStatus.Pending}"/>
                                <ComboBoxItem Content="Approved" Tag="{x:Static models:LeaveStatus.Approved}"/>
                                <ComboBoxItem Content="Rejected" Tag="{x:Static models:LeaveStatus.Rejected}"/>
                            </ComboBox>
                        </StackPanel>

                        <Button Grid.Column="1"
                                Content="Filter"
                                Command="{Binding LeaveFilterCommand}"
                                Style="{StaticResource PrimaryButton}"/>
                    </Grid>

                    <!-- Leave grid -->
                    <Border Grid.Row="1" Background="#090B17" CornerRadius="10" Padding="0">
                        <DataGrid x:Name="LeaveGrid"
                                  ItemsSource="{Binding LeaveRequests}"
                                  SelectedItem="{Binding SelectedLeave, Mode=TwoWay}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="False"
                                  SelectionUnit="FullRow"
                                  BorderThickness="0"
                                  Background="#090B17"
                                  GridLinesVisibility="None"
                                  RowHeight="48"
                                  ColumnHeaderHeight="44"
                                  SelectionChanged="LeaveGrid_SelectionChanged"
                                  RowEditEnding="LeaveGrid_RowEditEnding"
                                  CurrentCellChanged="LeaveGrid_CurrentCellChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID"       Binding="{Binding Id}"              IsReadOnly="True" Width="*"/>
                                <DataGridTextColumn Header="Employee" Binding="{Binding EmployeeDisplay}" IsReadOnly="True" Width="2*"/>
                                <DataGridTextColumn Header="Type"     Binding="{Binding LeaveType,  Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                                <DataGridTextColumn Header="Reason"   Binding="{Binding Reason,     Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>
                                <DataGridTextColumn Header="From"     Binding="{Binding DateFrom,   StringFormat=\{0:yyyy-MM-dd\}}" Width="*"/>
                                <DataGridTextColumn Header="To"       Binding="{Binding DateTo,     StringFormat=\{0:yyyy-MM-dd\}}" Width="*"/>
                                <DataGridCheckBoxColumn Header="Half Day" Binding="{Binding HalfDay, Mode=TwoWay}" Width="*"/>
                                <DataGridComboBoxColumn Header="Status"
                                                        ItemsSource="{StaticResource EditableStatuses}"
                                                        SelectedItemBinding="{Binding Status, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                        ElementStyle="{StaticResource CellCombo}"
                                                        EditingElementStyle="{StaticResource CellComboEditing}"
                                                        Width="*"/>
                                <DataGridTextColumn Binding="{Binding EmployeeId}"    Visibility="Collapsed"/>
                                <DataGridTextColumn Binding="{Binding ApproverUserId}" Visibility="Collapsed"/>
                                <DataGridTextColumn Binding="{Binding ApprovedAt, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" Visibility="Collapsed"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <!-- Action buttons -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                        <Button Content="Create"  Command="{Binding LeaveCreateCommand}"  Style="{StaticResource PrimaryButton}"   Margin="0,0,8,0"/>
                        <Button Content="Update"  Command="{Binding LeaveUpdateCommand}"  Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>
                        <Button Content="Delete"  Command="{Binding LeaveDeleteCommand}"  Style="{StaticResource DangerButton}"    Margin="0,0,8,0"/>
                        <Button Content="Approve" Command="{Binding LeaveApproveCommand}" Style="{StaticResource PrimaryButton}"   Margin="0,0,8,0"/>
                        <Button Content="Reject"  Command="{Binding LeaveRejectCommand}"  Style="{StaticResource DangerButton}"    Margin="0,0,8,0"/>
                        <Button Content="Cancel"  Command="{Binding LeaveCancelCommand}"  Style="{StaticResource SecondaryButton}"/>
                    </StackPanel>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
