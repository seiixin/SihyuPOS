<UserControl x:Class="SihyuPOSPayroll.Views.Admin.Tables.Tables"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SihyuPOSPayroll.ViewModels"
             Background="#000000">

    <UserControl.DataContext>
        <vm:TableViewModel/>
    </UserControl.DataContext>

    <Grid Margin="28,24,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <Border BorderBrush="#072219" BorderThickness="0,0,0,1" Margin="0,0,0,20" Padding="0,0,0,14">
            <TextBlock Text="Tables Management"
                       FontSize="26"
                       FontWeight="Bold"
                       Foreground="#477C60"/>
        </Border>

        <!-- Toolbar: Add + Refresh -->
        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Status legend -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="10" Height="10" CornerRadius="5" Background="#128854" Margin="0,0,6,0"/>
                <TextBlock Text="Available" Foreground="#9CA3AF" FontSize="13" Margin="0,0,16,0"/>
                <Border Width="10" Height="10" CornerRadius="5" Background="#EF4444" Margin="0,0,6,0"/>
                <TextBlock Text="Occupied"  Foreground="#9CA3AF" FontSize="13"/>
            </StackPanel>

            <!-- Action buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="+ Add Table"
                        Style="{StaticResource PrimaryButton}"
                        Margin="0,0,8,0"
                        Click="AddTable_Click"/>
                <Button Content="Refresh"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding RefreshCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Tables DataGrid -->
        <Border Grid.Row="2" Background="#090B17" CornerRadius="10" Padding="0">
            <DataGrid x:Name="TablesGrid"
                      ItemsSource="{Binding Tables}"
                      SelectedItem="{Binding Selected, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      BorderThickness="0"
                      Background="#090B17"
                      ColumnHeaderHeight="44"
                      RowHeight="48"
                      SelectionMode="Single"
                      SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"
                                        Binding="{Binding Id}"
                                        Width="80"/>
                    <DataGridTextColumn Header="Table Number"
                                        Binding="{Binding TableNumber}"
                                        Width="*"/>
                    <DataGridTemplateColumn Header="Status" Width="160">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="6" Padding="10,4"
                                        HorizontalAlignment="Left" Margin="8,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#128854"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Occupied">
                                                    <Setter Property="Background" Value="#EF4444"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Status}"
                                               Foreground="White"
                                               FontWeight="SemiBold"
                                               FontSize="12"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Actions" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Edit"
                                            Style="{StaticResource SecondaryButton}"
                                            Margin="0,0,6,0"
                                            Click="EditTable_Click"/>
                                    <Button Content="Delete"
                                            Style="{StaticResource DangerButton}"
                                            Click="DeleteTable_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- ADD / EDIT OVERLAY -->
        <Grid x:Name="EditorPanel"
              Visibility="Collapsed"
              Background="#AA000000"
              Grid.RowSpan="3">
            <Border Background="#090B17"
                    BorderBrush="#072219"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="28"
                    Width="380"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock x:Name="EditorTitle"
                               Text="Add Table"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="#477C60"
                               Margin="0,0,0,20"/>

                    <TextBlock Text="Table Number" Foreground="#9CA3AF" FontSize="13" Margin="0,0,0,6"/>
                    <TextBox x:Name="TableNumberBox" Margin="0,0,0,20"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel"
                                Style="{StaticResource SecondaryButton}"
                                Margin="0,0,8,0"
                                Click="CancelEditor_Click"/>
                        <Button Content="Save"
                                Style="{StaticResource PrimaryButton}"
                                Click="SaveEditor_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
