<UserControl x:Class="SihyuPOSPayroll.Views.Components.ToastNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="360" d:DesignHeight="56"
             HorizontalAlignment="Right"
             VerticalAlignment="Top"
             Margin="0,12,16,0"
             IsHitTestVisible="False">

    <UserControl.Resources>
        <!-- Slide-in + fade-in on Visible -->
        <Storyboard x:Key="ShowAnim">
            <DoubleAnimation Storyboard.TargetName="RootBorder"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.18"/>
            <DoubleAnimation Storyboard.TargetName="TranslateT"
                             Storyboard.TargetProperty="Y"
                             From="-20" To="0" Duration="0:0:0.18">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Fade-out -->
        <Storyboard x:Key="HideAnim" Completed="HideAnim_Completed">
            <DoubleAnimation Storyboard.TargetName="RootBorder"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.22"/>
        </Storyboard>
    </UserControl.Resources>

    <Border x:Name="RootBorder"
            CornerRadius="10"
            Padding="14,12"
            MinWidth="260"
            MaxWidth="380"
            Opacity="0">
        <Border.RenderTransform>
            <TranslateTransform x:Name="TranslateT" Y="-20"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" Color="#000000" ShadowDepth="0" Opacity="0.55"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Icon -->
            <TextBlock x:Name="IconText"
                       Grid.Column="0"
                       FontFamily="Segoe MDL2 Assets"
                       FontSize="16"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>

            <!-- Message -->
            <TextBlock x:Name="MessageText"
                       Grid.Column="1"
                       FontSize="13"
                       FontWeight="Medium"
                       Foreground="#F3F4F6"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap"/>
        </Grid>
    </Border>
</UserControl>
