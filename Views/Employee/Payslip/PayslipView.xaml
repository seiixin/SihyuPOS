<UserControl
    x:Class="SihyuPOSPayroll.Views.Employee.Payslip.PayslipView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:SihyuPOSPayroll.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="UserControl_Loaded"
    Background="#2A2B40">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#6378FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="40"/>
        </Style>
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:EmployeePayslipViewModel />
    </UserControl.DataContext>

    <Grid x:Name="Root"
          Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- title + toolbar -->
            <RowDefinition Height="Auto"/>
            <!-- search + request form -->
            <RowDefinition Height="*"/>
            <!-- lists -->
        </Grid.RowDefinitions>

        <!-- Title + toolbar -->
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,16">
            <TextBlock Text="?? My Payslips"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="White"
                       DockPanel.Dock="Left"/>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" Margin="8,0,0,0">
                <Button Content="Refresh"
                        Padding="12,6"
                        Margin="8,0,0,0"
                        Command="{Binding RefreshCommand}"/>
            </StackPanel>
        </DockPanel>

        <!-- Search + Request form -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Search -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="Search payslips"
                           Foreground="White"
                           Margin="0,0,0,6"/>
                <TextBox x:Name="SearchBox"
                         Padding="8"
                         FontSize="14"
                         VerticalContentAlignment="Center"
                         TextChanged="SearchBox_TextChanged"
                         Background="White"
                         Foreground="Black"
                         BorderThickness="1"
                         BorderBrush="#CCCCCC"/>
                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="LightGray"
                           Margin="0,6,0,0"/>
            </StackPanel>

            <!-- Request form -->
            <Border Grid.Column="1" Padding="12" CornerRadius="6" Background="#373955">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Request a Payslip"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="White"
                               Margin="0,0,0,8"/>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="Payroll ID (optional):"
                                   Width="160"
                                   VerticalAlignment="Center"
                                   Foreground="White"/>
                        <TextBox Width="120"
                                 Text="{Binding SelectedPayrollId, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="If you know the payroll row id, enter it; otherwise leave blank or 0."/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Margin="0,8,0,0">
                        <TextBlock Text="Reason (optional)"
                                   Foreground="White"/>
                        <TextBox Height="60"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 Text="{Binding RequestReason, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <Button Grid.Row="3"
                            Content="Request Payslip"
                            Margin="0,10,0,0"
                            Padding="12,6"
                            Command="{Binding SubmitRequestCommand}"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Lists: Payslips (left) + My Requests (right) -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Payslips -->
            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding Payslips}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      Background="#2A2B40"
                      Foreground="Black"
                      RowBackground="#D9D9D9"
                      AlternatingRowBackground="#D9D9D9"
                      BorderThickness="0"
                      GridLinesVisibility="None"
                      RowHeight="40"
                      ColumnHeaderHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Pay Date" Binding="{Binding PayDate, StringFormat=d}" Width="*" />
                    <DataGridTextColumn Header="Hours Worked" Binding="{Binding HoursWorked}" Width="*" />
                    <DataGridTextColumn Header="Rate / Hour" Binding="{Binding RatePerHour, StringFormat=?{0:N2}}" Width="*" />
                    <DataGridTextColumn Header="Deductions" Binding="{Binding Deductions, StringFormat=?{0:N2}}" Width="*" />
                    <DataGridTextColumn Header="Net Salary" Binding="{Binding NetSalary, StringFormat=?{0:N2}}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>

            <!-- My Requests -->
            <Border Grid.Column="1" Margin="12,0,0,0" Background="#373955" CornerRadius="6" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="My Payslip Requests"
                               Foreground="White"
                               FontWeight="SemiBold"
                               Margin="4,0,0,6"/>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding MyRequests}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              Background="#373955"
                              Foreground="Black"
                              RowBackground="#D9D9D9"
                              AlternatingRowBackground="#D9D9D9"
                              BorderThickness="0"
                              GridLinesVisibility="None"
                              RowHeight="36"
                              ColumnHeaderHeight="36">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding RequestDate, StringFormat=g}" Width="1.2*" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
                            <DataGridTextColumn Header="PayrollId" Binding="{Binding PayrollId}" Width="*" />
                            <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="2*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <!-- Busy overlay -->
        <Border Background="#80000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Loading..."
                       Foreground="White"
                       FontSize="18"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>